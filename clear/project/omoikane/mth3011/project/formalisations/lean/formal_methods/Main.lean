import FormalMethods

def main : IO Unit := do
  IO.println "formal_methods — MTH3011 project library."
  IO.println "Nothing to execute at the command line; the contents live in"
  IO.println "`FormalMethods/DeductionTheorem.lean` and `FormalMethods/Glivenko.lean`."
  IO.println "Open the project in VS Code with the Lean 4 extension to work interactively."
